<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 填充的颜色 -->
    <solid android:color="#f8f8f8" />
    <!-- 设置按钮的四个角为弧形 -->
    <!-- android:radius 弧形的半径 -->
    <corners android:topLeftRadius="@dimen/dp1"
        android:topRightRadius="@dimen/dp1"
        android:bottomRightRadius="@dimen/dp1"
        android:bottomLeftRadius="@dimen/dp1"/>
    <stroke
        android:width="@dimen/dp1"
        android:color="#f8f8f8"/>
</shape>